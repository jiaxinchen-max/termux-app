package com.termux.localgames.importer;

/** Stable, UI-localizable reasons contributing to candidate ranking. */
public enum CandidateSignal {
    ROOT_EXECUTABLE,
    ROOT_NAME_MATCH,
    LARGE_EXECUTABLE,
    LAUNCHER_NAME,
    DEEP_PATH,
    HELPER_NAME,
    SUPPORT_DIRECTORY
}
